export interface TeamMember {
  name: string;
  role: string;
  imageUrl: string;
}

export interface Service {
  id: string;
  name: string;
}

export interface NewsArticle {
  title: string;
  date: string;
  category: string;
  imageUrl: string;
  link: string;
}
